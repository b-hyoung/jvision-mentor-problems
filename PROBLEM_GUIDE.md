# 2학년 문제 출제 가이드

이 문서는 2학년 멘토가 문제를 출제할 때 따라야 할 형식과 절차를 설명합니다.

---

## 1. 브랜치 생성

```bash
git checkout -b mentor/{github-id}/problem-{번호}
# 예시
git checkout -b mentor/hyung/problem-002
```

---

## 2. 문제 파일 작성

`problems/problem{번호}.md` 파일을 아래 형식에 맞게 작성합니다.

### 필수 섹션

```markdown
# problem002

## 출제일
2026-04-10

## 문제 설명
정수 N을 입력받아 N줄에 걸쳐 별을 찍으세요.

## 입력
- 정수 1개 (1 ≤ N ≤ 100)

## 출력
- N줄에 걸쳐 별을 출력

## 테스트 케이스
```json
[
  {"input": "3", "output": "*\n**\n***"},
  {"input": "1", "output": "*"}
]
```

## 답안 위치
- `answer/problem002/{파일명}.py` (파일명 자유, PR 작성자 기준으로 정답 처리)
```

### 섹션 설명

| 섹션 | 필수 여부 | 설명 |
|------|-----------|------|
| `## 출제일` | 선택 | `YYYY-MM-DD` 형식. 없으면 오늘 날짜 기준으로 자동 배정. 해당 날짜에 이미 문제가 있으면 자동으로 +1일 |
| `## 문제 설명` | 필수 | 학생에게 보여줄 문제 설명 |
| `## 입력` | 권장 | 입력 조건 설명 |
| `## 출력` | 권장 | 출력 형식 설명 |
| `## 테스트 케이스` | 필수 | JSON 형식. AI 자동 채점에 사용됨 |
| `## 답안 위치` | 필수 | 학생이 파일을 어디에 올려야 하는지 안내 |

> `## 힌트 가이드` 같은 AI 전용 설정은 MD에 넣지 마세요.  
> 자동으로 `.github/problem-configs/problem{번호}.json` 에 생성됩니다.

---

## 3. 테스트 케이스 작성 요령

- **기본 테스트케이스**: 학생에게 공개되는 케이스. 2~3개면 충분.
- **심화 테스트케이스**: AI가 문제를 읽고 자동 생성. 직접 작성할 필요 없음.
- `input` 과 `output` 은 **문자열**로 작성.
- 줄바꿈은 `\n` 으로 표현.

```json
[
  {"input": "3", "output": "*\n**\n***"},
  {"input": "1", "output": "*"}
]
```

---

## 4. PR 생성

```bash
git add problems/problem002.md
git commit -m "feat: add problem002 - 별 찍기"
git push origin mentor/{github-id}/problem-{번호}
```

PR 제목 형식:
```
[problem-002] 별 찍기 문제 출제
```

---

## 5. 머지 후 자동 처리

다른 2학년 멘토가 승인 후 머지하면 아래가 자동으로 실행됩니다:

1. GPT가 문제를 읽고 AI 채점 설정 자동 생성
2. Cartel_Lab에 퀴즈 자동 등록 (출제일 기준)
3. `.github/problem-configs/problem002.json` 에 `cartel_quiz_id` 자동 기록

별도로 할 작업이 없습니다.

---

## 자주 하는 실수

| 실수 | 결과 |
|------|------|
| `## 테스트 케이스` 섹션 누락 | AI 채점 불가 |
| 테스트 케이스 JSON 형식 오류 | AI 채점 실패 |
| `output` 에 줄바꿈을 실제 줄바꿈으로 작성 | 파싱 오류, `\n` 으로 써야 함 |
| 답안 위치를 잘못 안내 | 학생이 잘못된 경로에 파일 제출 |
